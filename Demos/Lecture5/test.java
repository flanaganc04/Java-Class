package CS201.Demos.Lecture5;
//If files are saved in a folder you need to call its package and than make an instance of the class of the file name
public class test {
    //Logical Connectives 
        // and &  ampersand
        // or || pipe pipe 
        // xor: not using when both true returns false 
        // ! not 
    // For strings
    public static void main(String[] args) {
            String person = " officer"; 
            String greeting = "Hello";
            //String objects//
             String sentence = greeting + person;
                System.out.println(sentence);
            int n = greeting.length();
                System.out.println(n);
                System.out.println("\"Java\"");
                System.out.println("abc\\cde"); 
    }
}
        /**
         * Oh I love multiline comments
         * 
         */
        // s1.equals(s2)
    //Operators
        // abs() absolute value
        // ? -  max = (n1>n2) ? n1:n2
            /**This statement means if(n1>n2) {
               * max = n1
               * else {
               * max = n2
               * }
               * 
            } */
    //Syntax
        //static - no dynamic change 
        //final - makes a variables constant 
        // else if - do else if fisrt if is false, continue down the list of else if until all are false or one is true
    // vs code shortcuts
        // control shift quick comment
        // shift ontrol alt  

